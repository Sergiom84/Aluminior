# --- Datos reales de empresa: NUNCA al repositorio ---
*.mdb
*.accdb
*.ldb
*.laccdb
datos_reales/
export_datos/
# Export CSV del oraculo (EMP00xx): datos reales de clientes/presupuestos.
# Es el RUTA_CSV_ORIGEN; vive local, nunca al repo (ENTREGA §7.2).
EMP0016/
EMP[0-9][0-9][0-9][0-9]/

# --- Investigacion original: NADA de esta carpeta se versiona ---
# Contiene 818 MB de binarios de GAIA, configuraciones con credenciales y
# material propietario. Lo util (esquema, mapa de informes) ya vive en esquema/.
investigacion_productor_aluminio/

*.bmp
*.zip
*.msi
*.cab
*.chm
*.rpt

# --- Configuraciones con posibles credenciales o correos ---
log4net.config
*.config.orig

# --- Binarios de terceros (GAIA) ---
*.exe
*.dll
*.ocx

# --- Secretos ---
*.pfx
*.p12
*.key
*.config.local
appsettings.*.local.json

# Variables de entorno: TODAS las variantes, en cualquier carpeta.
# .env.example es la unica que se versiona.
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example

# --- Build / entorno ---
bin/
obj/
node_modules/
.vs/
*.user
__pycache__/
.venv/

# --- Next.js ---
.next/
out/
next-env.d.ts
*.tsbuildinfo
.vercel/

# --- Sistema ---
Thumbs.db
desktop.ini
